# Dependencies
node_modules/

# Build outputs
dist/
build/

# TypeScript cache
*.tsbuildinfo

# SQLite database (runtime data, not source)
data/
*.db
*.db-journal
*.db-wal
*.db-shm

# Environment files
.env
.env.local
.env.*.local

# Editor
.vscode/
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# OS
.DS_Store
Thumbs.db

# Vite
apps/web/.vite/

# pnpm
.pnpm-debug.log

# Reference videos / large media (local only)
reference/videos/
reference/2025 Playoff Media/

# Scratch / work-in-progress scorecards (use reference/ for canonical copies)
_tmp_scorecards/

# Private wolf-strategy cheat sheets + report cards + generator — local only, NEVER released/pushed
*Cheat-Sheet*.pdf
*Report-Card*.pdf
_tmp_cheatsheet*.html
_tmp_madden*.html
_tmp_josh*.html
_wolf-private/

# E2E (Playwright) — temp seeded DB, handoff, reports, artifacts
apps/tournament-web/e2e/.tmp/
apps/tournament-api/.e2e-tmp/
apps/tournament-web/playwright-report/
apps/tournament-web/test-results/
playwright/.cache/

# Claude Code local config (not source) — except orchestrator commands worth versioning.
# Use glob form (.claude/*) not directory form (.claude/) so git descends and the
# negations below can take effect.
.claude/*
!.claude/commands/
.claude/commands/*
!.claude/commands/tournament-director.md
!.claude/commands/codex-review.md
