# Story 3.1: React App Shell & Public Routes

Status: done

## Story

As a user (scorer, spectator, or admin),
I want a fast, installable web app with a working navigation shell and all route stubs in place,
so that every subsequent story has a consistent, type-safe foundation to build on.

## Acceptance Criteria

### Dependency Installation — FR61

1. `apps/web/package.json` adds all required runtime dependencies: `@tanstack/react-router`, `@tanstack/react-query`, `tailwindcss` (v4), `@tailwindcss/vite`, `vite-plugin-pwa`.

2. `apps/web/package.json` adds required dev dependencies: `@tanstack/router-plugin` (Vite plugin for file-based routing), `@tanstack/router-cli` (for `tsr generate` in typecheck), `@tanstack/react-query-devtools`.

### TanStack Router — File-Based Routing

3. `apps/web/vite.config.ts` adds `TanStackRouterVite({ target: 'react', autoCodeSplitting: true })` as the **first** plugin, before `react()`.

4. `apps/web/src/routes/__root.tsx` exports a `Route` created with `createRootRoute` containing the app shell layout: a top nav/header plus `<Outlet />` plus a bottom nav for mobile (links to `/` and `/standings`).

5. The following route files exist and each exports a `Route` created with `createFileRoute` (stub `component` is acceptable — just renders the page name in a `<div>`):
   - `src/routes/index.tsx` → `/` (Live Leaderboard)
   - `src/routes/standings.tsx` → `/standings` (YTD Standings)
   - `src/routes/score-entry.tsx` → `/score-entry` (Score Entry)
   - `src/routes/round.$roundId.tsx` → `/round/$roundId` (Round Detail)
   - `src/routes/admin.tsx` → `/admin` layout (wraps all admin routes)
   - `src/routes/admin/index.tsx` → `/admin/` (Admin Dashboard)
   - `src/routes/admin/login.tsx` → `/admin/login` (Admin Login)
   - `src/routes/admin/roster.tsx` → `/admin/roster`
   - `src/routes/admin/rounds.tsx` → `/admin/rounds`
   - `src/routes/admin/season.tsx` → `/admin/season`

6. `apps/web/src/routeTree.gen.ts` exists (auto-generated by `pnpm --filter @wolf-cup/web routes:generate` run before typecheck) and is committed/present in the repo.

### TanStack Query

7. `apps/web/src/lib/query-client.ts` exports a `QueryClient` instance with default options: `staleTime: 4000`, `gcTime: 300000`.

8. `apps/web/src/main.tsx` wraps the app in `<QueryClientProvider client={queryClient}>` with `<RouterProvider router={router} />` inside, replacing the old `<App />` mount.

### Tailwind v4

9. `apps/web/vite.config.ts` adds `tailwindcss()` (from `@tailwindcss/vite`) as a plugin.

10. `apps/web/src/index.css` contains `@import "tailwindcss";` (Tailwind v4 single-import syntax) and is imported in `main.tsx`.

### shadcn/ui

11. `apps/web/src/lib/utils.ts` exports a `cn` helper (from `clsx` + `tailwind-merge`) — created by `npx shadcn@latest init`.

12. `apps/web/src/components/ui/` directory exists with at least `button.tsx` added via `npx shadcn@latest add button`.

### Path Alias

13. `apps/web/tsconfig.app.json` has `"paths": { "@/*": ["./src/*"] }` under `compilerOptions`.

14. `apps/web/vite.config.ts` has `resolve: { alias: { '@': '/path/to/src' } }` (absolute path using `import.meta.dirname` or `fileURLToPath`).

### lib/api.ts

15. `apps/web/src/lib/api.ts` exports a typed `apiFetch` wrapper function that:
    - Prefixes all paths with `/api`
    - Accepts generic `<T>` type parameter and returns `Promise<T>`
    - On non-ok responses, throws an `Error` with the `code` from the response JSON if present

### PWA Foundation

16. `apps/web/vite.config.ts` includes `VitePWA({ registerType: 'autoUpdate', manifest: { name: 'Wolf Cup', short_name: 'Wolf Cup', display: 'standalone', theme_color: '#1a1a1a', background_color: '#ffffff', icons: [...] } })`.

17. `apps/web/index.html` has `<link rel="manifest" href="/manifest.webmanifest" />` and `<meta name="theme-color" content="#1a1a1a" />`.

### Build & Quality

18. `pnpm --filter @wolf-cup/web routes:generate` (calls `tsr generate`) runs with zero errors.

19. `pnpm --filter @wolf-cup/web typecheck` passes with zero TypeScript errors.

20. `pnpm --filter @wolf-cup/web lint` passes with zero ESLint errors.

21. `pnpm --filter @wolf-cup/web build` completes successfully with no errors.

## Tasks / Subtasks

- [x] Task 1: Install dependencies and update vite.config.ts (AC: #1, #2, #3, #9, #13, #14, #16)
  - [x] Add runtime deps: `@tanstack/react-router`, `@tanstack/react-query`, `tailwindcss`, `@tailwindcss/vite`, `vite-plugin-pwa`
  - [x] Add dev deps: `@tanstack/router-plugin`, `@tanstack/router-cli`, `@tanstack/react-query-devtools`
  - [x] Update `vite.config.ts`: add `TanStackRouterVite` (first), `tailwindcss()`, `VitePWA({...})`, path alias
  - [x] Update `tsconfig.app.json`: add `baseUrl` + `paths` for `@/*`
  - [x] Add `routes:generate` script to `package.json`: `"routes:generate": "tsr generate"`
  - [x] Update `typecheck` script to: `"typecheck": "tsr generate && tsc --noEmit -p tsconfig.app.json"`

- [x] Task 2: Set up Tailwind v4 and shadcn/ui (AC: #10, #11, #12)
  - [x] Create `src/index.css` with `@import "tailwindcss";` + zinc theme CSS variables
  - [x] Created shadcn files manually (network prevented CLI fetch); installed `class-variance-authority`, `@radix-ui/react-slot`, `lucide-react`
  - [x] Created `src/lib/utils.ts` (cn helper), `src/components/ui/button.tsx`, `components.json`

- [x] Task 3: Create app shell and lib files (AC: #7, #8, #15)
  - [x] Create `src/lib/query-client.ts` with `QueryClient` (staleTime: 4000, gcTime: 300000)
  - [x] Create `src/lib/api.ts` with typed `apiFetch` wrapper
  - [x] Update `src/main.tsx`: import CSS, import router + query client, wrap in providers; deleted App.tsx

- [x] Task 4: Create all route files (AC: #4, #5)
  - [x] Create `src/routes/__root.tsx` (app shell: header + `<Outlet />` + mobile bottom nav)
  - [x] Create `src/routes/index.tsx` (stub: "Live Leaderboard")
  - [x] Create `src/routes/standings.tsx` (stub: "Season Standings")
  - [x] Create `src/routes/score-entry.tsx` (stub: "Score Entry")
  - [x] Create `src/routes/round.$roundId.tsx` (stub: "Round Detail")
  - [x] Create `src/routes/admin.tsx` (admin layout with `<Outlet />`)
  - [x] Create `src/routes/admin/index.tsx` (stub: "Admin Dashboard")
  - [x] Create `src/routes/admin/login.tsx` (stub: "Admin Login")
  - [x] Create `src/routes/admin/roster.tsx` (stub: "Roster")
  - [x] Create `src/routes/admin/rounds.tsx` (stub: "Rounds")
  - [x] Create `src/routes/admin/season.tsx` (stub: "Season")

- [x] Task 5: Generate route tree and update index.html (AC: #6, #17)
  - [x] Run `pnpm --filter @wolf-cup/web routes:generate` → produced `src/routeTree.gen.ts` (11 routes)
  - [x] Update `index.html`: add manifest link, theme-color meta tag, PWA apple meta tags

- [x] Task 6: Build quality checks (AC: #18, #19, #20, #21)
  - [x] `pnpm --filter @wolf-cup/web routes:generate` — zero errors
  - [x] `pnpm --filter @wolf-cup/web typecheck` — zero TypeScript errors
  - [x] `pnpm --filter @wolf-cup/web lint` — zero ESLint errors
  - [x] `pnpm --filter @wolf-cup/web build` — successful Vite production build (185 modules, PWA service worker generated)

## Dev Notes

### What Already Exists in `apps/web`

```
apps/web/
├── package.json          ← @wolf-cup/web; React 19, Vite ^6, @vitejs/plugin-react
├── vite.config.ts        ← just react() plugin — needs TanStackRouterVite, tailwindcss, VitePWA
├── tsconfig.json         ← references tsconfig.app.json
├── tsconfig.app.json     ← strict TypeScript for src/ — needs path alias added
├── index.html            ← bare: title "Wolf Cup", #root div
└── src/
    ├── main.tsx          ← StrictMode + createRoot(<App />) — replace entirely
    └── App.tsx           ← <div>Wolf Cup</div> — DELETE this file
```

**`App.tsx` must be deleted** — it is replaced by the TanStack Router file-based routing setup. `main.tsx` will no longer import it.

### Exact Package Versions to Install

Use `pnpm --filter @wolf-cup/web add` for runtime, `pnpm --filter @wolf-cup/web add -D` for devDeps:

**Runtime:**
```
@tanstack/react-router       # latest v1.x (file-based, type-safe)
@tanstack/react-query        # latest v5.x
tailwindcss                  # v4.x  ← NOT v3
@tailwindcss/vite            # v4.x  ← the Vite-native plugin (replaces postcss config)
vite-plugin-pwa              # latest v0.x
clsx                         # for cn() helper (shadcn)
tailwind-merge               # for cn() helper (shadcn)
```

**Dev:**
```
@tanstack/router-plugin      # Vite plugin for file-based route generation
@tanstack/router-cli         # CLI for `tsr generate` (typecheck script)
@tanstack/react-query-devtools  # dev only
```

**⚠️ Tailwind v4 is fundamentally different from v3:**
- No `tailwind.config.js` — configuration is done in CSS via `@theme` directives
- No `@tailwind base/components/utilities` — replaced by `@import "tailwindcss"`
- The Vite plugin `@tailwindcss/vite` replaces PostCSS integration
- shadcn/ui v2.x supports Tailwind v4 natively via `npx shadcn@latest init`

### vite.config.ts Final Shape

```ts
import { defineConfig } from 'vite';
import { TanStackRouterVite } from '@tanstack/router-plugin/vite';
import react from '@vitejs/plugin-react';
import tailwindcss from '@tailwindcss/vite';
import { VitePWA } from 'vite-plugin-pwa';
import { fileURLToPath } from 'node:url';

export default defineConfig({
  plugins: [
    // TanStack Router MUST be first (generates routeTree.gen.ts before React transform)
    TanStackRouterVite({ target: 'react', autoCodeSplitting: true }),
    react(),
    tailwindcss(),
    VitePWA({
      registerType: 'autoUpdate',
      manifest: {
        name: 'Wolf Cup',
        short_name: 'Wolf Cup',
        description: 'Harvey Cup Golf League Scorer',
        display: 'standalone',
        orientation: 'portrait',
        theme_color: '#1a1a1a',
        background_color: '#ffffff',
        start_url: '/',
        icons: [
          { src: '/icon-192.png', sizes: '192x192', type: 'image/png' },
          { src: '/icon-512.png', sizes: '512x512', type: 'image/png' },
        ],
      },
    }),
  ],
  resolve: {
    alias: {
      '@': fileURLToPath(new URL('./src', import.meta.url)),
    },
  },
});
```

### tsconfig.app.json Path Alias Addition

Add under `compilerOptions`:
```json
"baseUrl": ".",
"paths": {
  "@/*": ["./src/*"]
}
```

### TanStack Router — File-Based Routing Setup

**How it works:**
- The `TanStackRouterVite` plugin watches `src/routes/` and auto-generates `src/routeTree.gen.ts`
- During `vite dev` / `vite build`: auto-generated on file changes
- For `tsc --noEmit` (no Vite): must run `tsr generate` first (that's why `routes:generate` script is needed)
- **Naming conventions:**
  - `src/routes/__root.tsx` — root layout (required)
  - `src/routes/index.tsx` → `/`
  - `src/routes/standings.tsx` → `/standings`
  - `src/routes/round.$roundId.tsx` → `/round/$roundId` (dollar = param)
  - `src/routes/admin.tsx` → layout route for the `/admin` subtree
  - `src/routes/admin/index.tsx` → `/admin/` (index of admin)
  - `src/routes/admin/login.tsx` → `/admin/login`

**`src/routes/__root.tsx` — app shell:**
```tsx
import { createRootRoute, Outlet, Link } from '@tanstack/react-router';

export const Route = createRootRoute({
  component: () => (
    <div className="flex flex-col min-h-screen bg-background text-foreground">
      {/* App header */}
      <header className="sticky top-0 z-50 bg-background border-b px-4 py-3">
        <h1 className="text-lg font-bold">🐺 Wolf Cup</h1>
      </header>

      {/* Page content */}
      <main className="flex-1 overflow-y-auto">
        <Outlet />
      </main>

      {/* Mobile bottom nav */}
      <nav className="sticky bottom-0 border-t bg-background grid grid-cols-3 divide-x">
        <Link to="/" className="flex flex-col items-center py-3 text-xs [&.active]:font-bold">
          <span>🏆</span>Leaderboard
        </Link>
        <Link to="/standings" className="flex flex-col items-center py-3 text-xs [&.active]:font-bold">
          <span>📊</span>Standings
        </Link>
        <Link to="/score-entry" className="flex flex-col items-center py-3 text-xs [&.active]:font-bold">
          <span>⛳</span>Score
        </Link>
      </nav>
    </div>
  ),
});
```

**Stub route pattern (all non-root routes):**
```tsx
import { createFileRoute } from '@tanstack/react-router';

export const Route = createFileRoute('/standings')({
  component: () => <div className="p-4"><h2>Season Standings</h2><p>Coming soon.</p></div>,
});
```

**Admin layout (`src/routes/admin.tsx`):**
```tsx
import { createFileRoute, Outlet } from '@tanstack/react-router';

export const Route = createFileRoute('/admin')({
  component: () => (
    <div>
      <div className="bg-amber-50 border-b px-4 py-2 text-sm text-amber-800">Admin Panel</div>
      <Outlet />
    </div>
  ),
});
```

**Route for dynamic param (`src/routes/round.$roundId.tsx`):**
```tsx
import { createFileRoute } from '@tanstack/react-router';

export const Route = createFileRoute('/round/$roundId')({
  component: function RoundDetail() {
    const { roundId } = Route.useParams();
    return <div className="p-4"><h2>Round {roundId}</h2><p>Coming soon.</p></div>;
  },
});
```

### main.tsx — Final Shape

```tsx
import { StrictMode } from 'react';
import { createRoot } from 'react-dom/client';
import { RouterProvider, createRouter } from '@tanstack/react-router';
import { QueryClientProvider } from '@tanstack/react-query';
import { routeTree } from './routeTree.gen';
import { queryClient } from './lib/query-client';
import './index.css';

const router = createRouter({ routeTree });

// Type registration — required for TanStack Router type safety
declare module '@tanstack/react-router' {
  interface Register {
    router: typeof router;
  }
}

const root = document.getElementById('root');
if (!root) throw new Error('Root element not found');

createRoot(root).render(
  <StrictMode>
    <QueryClientProvider client={queryClient}>
      <RouterProvider router={router} />
    </QueryClientProvider>
  </StrictMode>,
);
```

### lib/query-client.ts

```ts
import { QueryClient } from '@tanstack/react-query';

export const queryClient = new QueryClient({
  defaultOptions: {
    queries: {
      staleTime: 4000,     // 4 seconds — leaderboard stays fresh
      gcTime: 300000,      // 5 minutes — cache cleanup
      retry: 1,
    },
  },
});
```

### lib/api.ts

```ts
const BASE = '/api';

export async function apiFetch<T>(path: string, init?: RequestInit): Promise<T> {
  const res = await fetch(`${BASE}${path}`, {
    headers: { 'Content-Type': 'application/json', ...init?.headers },
    ...init,
  });
  if (!res.ok) {
    const body = await res.json().catch(() => ({})) as { code?: string; error?: string };
    throw new Error(body.code ?? body.error ?? `HTTP ${res.status}`);
  }
  return res.json() as Promise<T>;
}
```

### src/index.css

```css
@import "tailwindcss";

/* shadcn/ui CSS variables injected here by `npx shadcn@latest init` */
```

### shadcn/ui Init

When running `npx shadcn@latest init`, answer:
- Style: **Default**
- Base color: **Zinc** (works well for dark golf course use)
- CSS variables for colors: **Yes**
- TypeScript: **Yes**
- Path alias: `@/components`

This creates:
- `src/lib/utils.ts` — `cn()` helper with `clsx` + `tailwind-merge`
- `src/components/ui/` directory
- Updates `src/index.css` with CSS variable theme definitions
- Possibly updates `vite.config.ts` and `tsconfig.app.json` (verify after init)

Then run `npx shadcn@latest add button` to get at least one component to verify the setup.

### ESLint Configuration

The root `eslint.config.js` already covers `apps/web/src`. No separate ESLint config needed for the web app (it inherits the monorepo root config). Verify with `pnpm --filter @wolf-cup/web lint`.

### PWA Icons

For Story 3.1, create placeholder PNG icons at `apps/web/public/`:
- `icon-192.png` — 192×192px solid dark background with "WC" text (simple placeholder)
- `icon-512.png` — 512×512px same

Use any minimal image generation (even a 1px placeholder or copy from a stock icon). Real branded icons come in Epic 6.

### index.html PWA Updates

```html
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="theme-color" content="#1a1a1a" />
    <meta name="apple-mobile-web-app-capable" content="yes" />
    <meta name="apple-mobile-web-app-status-bar-style" content="black-translucent" />
    <meta name="apple-mobile-web-app-title" content="Wolf Cup" />
    <link rel="apple-touch-icon" href="/icon-192.png" />
    <link rel="manifest" href="/manifest.webmanifest" />
    <title>Wolf Cup</title>
  </head>
  <body>
    <div id="root"></div>
    <script type="module" src="/src/main.tsx"></script>
  </body>
</html>
```

### package.json Script Updates

```json
"scripts": {
  "routes:generate": "tsr generate",
  "typecheck": "tsr generate && tsc --noEmit -p tsconfig.app.json",
  "lint": "eslint src",
  "dev": "vite",
  "build": "vite build",
  "preview": "vite preview"
}
```

### What This Story Does NOT Include

- No real data fetching (all routes are stubs)
- No admin authentication implementation (Story 3.x)
- No score entry UI (Stories 3.2–3.5)
- No leaderboard data (Story 3.8)
- No offline queue (Story 3.7)
- No service worker cache strategies beyond basic manifest (Story 6.1)
- No Vitest for React components (not required in architecture for apps/web)

### Architecture References

- [Source: architecture.md#Frontend Technology Stack] — Vite, React 19, TanStack Router, TanStack Query, Tailwind v4, shadcn/ui, vite-plugin-pwa
- [Source: architecture.md#API Communication Patterns] — `refetchInterval: 5000`, `staleTime: 4000`, `/api` prefix
- [Source: architecture.md#Web Package Structure] — exact folder layout for routes/, components/, hooks/, lib/
- [Source: architecture.md#Public Routes and URL Structure] — all route paths
- FR61: Public app at wolf.dagle.cloud (no auth for read-only views)
- NFR17: PWA app shell loads from cache instantly on repeat visits

## Dev Agent Record

### Agent Model Used

claude-sonnet-4-6

### Debug Log References

- shadcn@latest init failed with ECONNRESET (network sandbox restriction fetching https://ui.shadcn.com/r/styles/index.json). Created all shadcn files manually: components.json, src/lib/utils.ts, src/index.css (zinc theme, Tailwind v4 format), src/components/ui/button.tsx. Installed class-variance-authority, @radix-ui/react-slot, lucide-react as additional deps.
- shadcn validates import alias from tsconfig.json (root), not tsconfig.app.json. Added compilerOptions.paths to both files.

### Completion Notes List

- All 21 ACs satisfied. 185 modules built, zero TS errors, zero ESLint errors.
- TanStackRouterVite placed first in vite plugins as required; autoCodeSplitting:true generates per-route chunks visible in build output.
- Tailwind v4 setup: @import "tailwindcss" + @theme inline for color token mapping + zinc oklch variables. No postcss/tailwind.config.js needed.
- shadcn files created manually (network restriction); zinc theme with oklch CSS vars is equivalent to what `shadcn@latest init --defaults` would produce.
- PWA: vite-plugin-pwa generateSW mode; service worker and workbox bundle produced in dist/. 17 entries precached.
- Placeholder PNG icons (192px, 512px) created with Python: solid #1a1a1a background.
- ✅ Resolved review finding [MEDIUM]: apiFetch header override bug — moved `...init` before `headers:{}` so merged Content-Type default is never overridden
- ✅ Resolved review finding [MEDIUM]: eslint.config.js added to File List (was modified in earlier story, omitted from Story 3.1 documentation)
- ✅ Resolved review finding [LOW]: apiFetch path leading-slash guard added — throws immediately on missing `/`
- ✅ Resolved review finding [LOW]: PWA manifest icons now include `purpose: 'any maskable'`
- ✅ Resolved review finding [LOW]: ReactQueryDevtools added to main.tsx (renders in dev builds)

### File List

- `apps/web/package.json` — updated (add dependencies + scripts)
- `apps/web/vite.config.ts` — updated (TanStackRouterVite, tailwindcss, VitePWA, path alias)
- `apps/web/tsconfig.app.json` — updated (baseUrl, paths for @/*)
- `apps/web/tsconfig.json` — updated (added compilerOptions.paths for shadcn alias detection)
- `apps/web/components.json` — new (shadcn config)
- `apps/web/index.html` — updated (PWA meta tags, manifest link)
- `apps/web/src/main.tsx` — updated (RouterProvider + QueryClientProvider, delete App import)
- `apps/web/src/App.tsx` — DELETED
- `apps/web/src/index.css` — new (@import "tailwindcss" + shadcn zinc CSS vars, Tailwind v4 format)
- `apps/web/src/routeTree.gen.ts` — new (auto-generated by tsr generate)
- `apps/web/src/lib/query-client.ts` — new
- `apps/web/src/lib/api.ts` — new
- `apps/web/src/lib/utils.ts` — new (shadcn cn() helper)
- `apps/web/src/components/ui/button.tsx` — new (shadcn button component)
- `apps/web/src/routes/__root.tsx` — new (app shell)
- `apps/web/src/routes/index.tsx` — new (stub: Live Leaderboard)
- `apps/web/src/routes/standings.tsx` — new (stub: Standings)
- `apps/web/src/routes/score-entry.tsx` — new (stub: Score Entry)
- `apps/web/src/routes/round.$roundId.tsx` — new (stub: Round Detail)
- `apps/web/src/routes/admin.tsx` — new (admin layout)
- `apps/web/src/routes/admin/index.tsx` — new (stub: Admin Dashboard)
- `apps/web/src/routes/admin/login.tsx` — new (stub: Admin Login)
- `apps/web/src/routes/admin/roster.tsx` — new (stub: Roster)
- `apps/web/src/routes/admin/rounds.tsx` — new (stub: Rounds)
- `apps/web/src/routes/admin/season.tsx` — new (stub: Season)
- `apps/web/public/icon-192.png` — new (placeholder PWA icon 192×192)
- `apps/web/public/icon-512.png` — new (placeholder PWA icon 512×512)
- `eslint.config.js` — updated (adds `**/*.js` ignore + `@typescript-eslint/no-unused-vars` rule; modified in earlier story)
