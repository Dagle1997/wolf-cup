# Session Handoff — 2026-06-23 (Tournament)

**For:** next session · **Branch:** master · **Prod:** tournament.dagle.cloud (live; nothing deployed this session)

---

## TL;DR
This session: (1) shipped the **dark-mode P0 contrast/a11y fixes** from the Gemini UI review, (2) committed leftover sub-status review docs, (3) ran the **tournament-director** and completed **Story 3-2 (scorecard API)** end-to-end. All work is **committed locally, NOT pushed** (pushing is a Josh action; also gated by the global no-external-push rule). Next up is the **Epic-3 chain toward the Pete Dye brochure re-capture**: **3-3 → 3-4 → capture**.

## Commits this session (local, unpushed — ahead of origin/master)
1. **`f5147a8`** — `fix(tournament-web)`: dark-mode contrast + a11y. Two High contrast bugs (Live-Round CTA white-on-amber → `--color-on-accent`; HoleBadge red/amber/blue → theme-adapting `--color-badge-*` tokens, light mode pixel-identical) + 4 Mediums (ScrollableTable on the scorecard grid; iOS safe-area inset on global-nav; storm-banner functional-merge; 44px tap target on the past/cancelled toggle). tournament-web 404/404 green, tsc clean.
2. **`d49b098`** — `docs(reviews)`: the leftover codex+gemini+synthesis trio for the prior "sub-status fix" (committed at Josh's direction to clear the dirty tree before the director ran).
3. **`968f3f3`** — `tournament: 3-2 scorecard API` — **DONE** (sprint-status flipped to `done` atomically in this commit). See below.

## Story 3-2 — Scorecard API (DONE)
- **New endpoint:** `GET /api/rounds/:roundId/players/:playerId/scorecard` → `{ holes: ScorecardHole[] }`. Feeds the during-round Wolf-style board (3-1 `HoleBadge`/`ScorecardGrid`; wired to a route in **3-4**).
- **Files:** `apps/tournament-api/src/services/scorecard.ts` (pure `buildPlayerScorecard`) + `routes/scorecard.ts` + 2 test files + `app.ts` mount. **No schema, no migration, no web change.**
- **Key behavior:** `relativeStrokes` from the **pinned `ch`** via `allocateStrokesFromCourseHandicap` (same kernel the money path uses — scorecard net == money net by construction); net = gross − strokes, null when unplayed; **no-pin/null-ch ⇒ netScore null (not net=gross)**; claims via `deriveCurrentClaims`; **`moneyNet` always null (the 3-3 seam)**; `Cache-Control: no-store`. Auth = participant-or-organizer (tenant-scoped) → 403; target-not-in-round → 404.
- **Reviews:** spec FIXED (2 High resolved: 9-hole range, silent net=gross), impl synthesis **SHIP (high)**, party SHIP. All review artifacts in `_bmad-output/reviews/3-2-*`.
- **3-2 followups (non-blocking, in the story's Dev Agent Record):** money audience-bounding at 3-3; course-revision agreement (`event_rounds` vs `round_pin`); back-9 9-hole rounds (needs a schema field); reachability test + TOCTOU-404 (both theoretical).

## ▶ NEXT: resume the Epic-3 chain
Goal = a **native Pete Dye brochure re-capture** (p2/p3/p4/p5) once the scorecard renders on a live route. **Trip is Jun 26–27.**

Epic-3 status (sprint-status.yaml):
- `3-1` HoleBadge/grid port — **done** (prior session)
- `3-2` scorecard API — **done** (this session, `968f3f3`)
- **`3-3` per-hole F1 money — NEXT.** 🔴 **KEY DECISION (Josh, 2026-06-23): BUILD REAL per-hole F1 money — NOT the flagged deferral.** Pull the Epic-4 per-hole seam forward; **golden hand-calc gated** — Josh approves the golden BEFORE settlement code merges (NFR-C1). This fills the `moneyNet` field the 3-2 API currently returns as null. The seam: `computeF1FoursomeResults` in `money-detail.ts` zeroes per-hole money today.
- `3-4` during-round leaderboard rework — wires `ScorecardGrid` into a live route (this is what makes p4 capturable).
- `3-5` weekend standings (sortable Money/Net-to-Par/Individual).

**To resume:** fresh session → `/tournament-director` (or `/loop /tournament-director`). It auto-picks the next backlog story = **3-3**. Re-supply the KEY DECISION above (build real per-hole money, golden-gated) so the 3-3 spec is shaped correctly.

> ⚠️ **Epic-3 numbering collision (important for whoever drives the director):** the create-story `epics_file` (`epics-f1-rules-games.md`) has a DIFFERENT "Epic 3: Teams & the Event Pot" with its own 3.1–3.5. The scoreboard Epic-3 (3-1..3-5) is specified by **`scoreboard-rework-spec.md`**, NOT the epics file. Source 3-3 from `scoreboard-rework-spec.md` (+ the F1 money-detail/games-money code), like 3-2 was. Do not let create-story resolve "3.3" against the epics file (wrong story).

## Other state / cautions
- **Brochure (P1):** blocked on 3-4 for the p4 scorecard shot (the only shot still showing a Wolf Cup image). After 3-4 lands, capture per `reference/SCREENSHOTS.md` → `node reference/swap-and-render.mjs`. Dark-mode fixes (`f5147a8`) are in, so the dark p4/p5 screens are ready to shoot. Humor pass still needs Josh's inside-jokes.
- **Known test flake (NOT a regression):** `round-lifecycle.integration.test.ts` intermittently fails under full-suite load with `SQLITE_ERROR: no such table` (the documented `file::memory:?cache=shared` race). Passes **24/24 clean in isolation**. Don't chase it. (My new scorecard tests use a private `:memory:` DB specifically to avoid this.)
- **Director config:** `.director-config.json` has `auto_approve_clean_specs: true`. 3-2 didn't qualify (had Highs + fixes); a money story like 3-3 likely won't either.
- **Nothing deployed.** Prod unchanged. Demo event `ba5153d1…` from the prior session still live.
