# Codex Review

- Generated: 2026-04-28T13:12:14.708Z
- Model: gpt-5.2
- Reasoning effort: medium
- Workspace root: D:\wolf-cup
- Reviewed files: _bmad-output/implementation-artifacts/tournament/T5-3-offline-queue-indexeddb-client-event-id-idempotency.md

## Summary

Spec is mostly concrete and stays within the stated ALLOWED footprint, but there are a few internal contradictions and underspecified contracts that could lead to incompatible implementations (especially around who generates `clientEventId`, how terminal errors are detected, and what `resolveConflict('overwrite')` mutates). The kind-agnostic dispatch approach is defensible, but it pushes several correctness requirements onto consumers and needs tighter contracts to prevent “queue stuck forever” failure modes.

Overall risk: medium

## Findings

1. [high] Contradiction: spec says library generates `clientEventId`, but AC + tests require caller to supply it (and missing should reject)
   - File: _bmad-output/implementation-artifacts/tournament/T5-3-offline-queue-indexeddb-client-event-id-idempotency.md:63-67
   - Confidence: high
   - Why it matters: Lines 65-67 state UUID v4 is generated at enqueue time and must be preserved across retries. But AC #1’s `enqueueMutation(...)` requires `clientEventId` in the input type (line 107), and the tests explicitly require rejecting an entry missing `clientEventId` (line 190). This is mutually exclusive and will cause divergent implementations (either library-generated IDs or consumer-generated IDs), which directly impacts idempotency guarantees and downstream consumer specs (T5-2) and DB unique constraints assumptions.
   - Suggested fix: Pick one and make it consistent everywhere:
- Option A (recommended for safety): `enqueueMutation` generates `clientEventId` if absent and returns it (change signature to return `{id, clientEventId}` or the full stored entry). Update followups + tests accordingly.
- Option B: Consumer generates and passes `clientEventId`; remove claims that T5-3 generates it (lines 65-67) and adjust wording to “generated by the caller once and persisted in the queue entry.”

2. [high] Terminal error classification is ambiguous/incorrectly sourced (uses `entry.body.code` vs response code)
   - File: _bmad-output/implementation-artifacts/tournament/T5-3-offline-queue-indexeddb-client-event-id-idempotency.md:147-158
   - Confidence: high
   - Why it matters: AC #4 says terminal classification is based on “`entry.body.code` (or response body's `code` field)” (line 156). `entry.body` is the *request payload*, not an error payload, so `entry.body.code` will often be undefined or unrelated. Additionally, the spec doesn’t define whether the drain must parse JSON error bodies, what to do on non-JSON responses, or where exactly `code` is expected. This can produce inconsistent behavior (purging vs retrying) and “queue pile-up forever” in practice.
   - Suggested fix: Specify a single source of truth:
- Parse `await response.json()` when possible; extract `code` from the response body only.
- Define fallback behavior if JSON parsing fails (treat as transient vs terminal).
- Remove `entry.body.code` from the contract unless you truly intend request-driven classification (unusual).

3. [high] `resolveConflict('overwrite')` mutation contract is internally inconsistent and too loosely defined for `body: unknown`
   - File: _bmad-output/implementation-artifacts/tournament/T5-3-offline-queue-indexeddb-client-event-id-idempotency.md:111-113
   - Confidence: high
   - Why it matters: AC #1 hard-requires `resolveConflict('overwrite')` to mutate the entry body by setting `overwriteFlag: true` (line 112). But later the spec says the exact field name is T5.10’s decision and T5-3 is a “one-line change” if it differs (lines 255-256). That’s a spec-level mismatch that can break consumers/tests if T5.10 chooses a different wire shape. Also, `body` is typed `unknown` (line 106), so mutating it assumes it is an object; this needs runtime guarding to avoid corrupting entries or throwing during conflict resolution.
   - Suggested fix: Make the contract explicit and stable:
- Either lock the field name now (and ensure T5.10 + consumers conform), or parameterize it (e.g., `resolveConflict(id, { resolution: 'overwrite', patchBody: (body) => newBody })` or store a per-kind conflict strategy).
- Add runtime checks: only patch if `typeof body === 'object' && body !== null` (and decide what happens otherwise—likely quarantine).

4. [medium] Fetch contract underspecified: missing JSON headers / response parsing expectations may cause non-interoperable drains
   - File: _bmad-output/implementation-artifacts/tournament/T5-3-offline-queue-indexeddb-client-event-id-idempotency.md:51-52
   - Confidence: medium
   - Why it matters: The drain is specified as `fetch(entry.url, { method: 'POST', body: JSON.stringify(entry.body), credentials: 'include' })` (line 51). Without `Content-Type: application/json`, many backends won’t parse JSON. Also, AC #4 needs access to “response body” for `lastError` and terminal classification (lines 155-156), but doesn’t specify whether to read text vs json, and how to avoid double-reading the stream. Different implementations here will change behavior and make tests brittle.
   - Suggested fix: Pin the contract:
- Require `headers: { 'Content-Type': 'application/json' }` (and optionally `Accept: 'application/json'`).
- Define error-body extraction: `safeJsonParse(await response.text())` or `try { await response.json() } catch { await response.text() }` and store a consistent shape in `lastError.body`.

5. [medium] Corrupted-entry quarantine semantics conflict with transient-error semantics and “continue” requirement
   - File: _bmad-output/implementation-artifacts/tournament/T5-3-offline-queue-indexeddb-client-event-id-idempotency.md:159-174
   - Confidence: high
   - Why it matters: AC #4 says malformed entry → `quarantineEntry(entry.id)` (line 159) but doesn’t state whether drain should continue or break. AC #6 explicitly requires “drain CONTINUES to the next entry” for corrupted entries (line 173). This discrepancy is small but can lead to a drain that stops on the first corrupted entry (starving valid entries behind it).
   - Suggested fix: Align AC #4 with AC #6: explicitly state “quarantine and continue processing remaining entries” in AC #4’s bullet list.

6. [medium] Consumer-registration hazard for terminal errors is acknowledged but not enforced by library; consider a failsafe to prevent infinite retry on deterministic 4xx
   - File: _bmad-output/implementation-artifacts/tournament/T5-3-offline-queue-indexeddb-client-event-id-idempotency.md:53-54
   - Confidence: medium
   - Why it matters: Default terminal list is empty (line 53), and transient behavior for non-terminal 4xx is to break and retry later (line 157). If a consumer forgets to register terminal codes, deterministic validation errors will cause the queue to retry forever and block subsequent entries (since the loop breaks). The spec notes this risk (lines 251-253), but mitigation is only “T5-2 MUST include registration” (line 252), which is easy to miss and doesn’t protect future consumers.
   - Suggested fix: Add a library-level failsafe/AC, e.g.:
- Treat specific HTTP statuses as terminal by default (400/401/403/404/422) unless explicitly opted into retry, or
- Add a max retry count / exponential backoff / “dead-letter after N attempts”, or
- Do not `break` the drain on 4xx; instead, quarantine or mark errored if unrecognized (prevents queue-wide blockage).

7. [low] Window conflict event should be specified as `CustomEvent` with `detail` payload for consistent consumption and testing
   - File: _bmad-output/implementation-artifacts/tournament/T5-3-offline-queue-indexeddb-client-event-id-idempotency.md:52-53
   - Confidence: high
   - Why it matters: Spec says “emits a custom event on window carrying {…}” (line 52) but doesn’t specify `CustomEvent` vs `Event` with side channel. React/TanStack Query consumers and tests typically rely on `window.addEventListener(name, (e) => (e as CustomEvent).detail)`. Without pinning this, implementations may diverge and downstream consumers will be brittle.
   - Suggested fix: Specify: `window.dispatchEvent(new CustomEvent('tournament-offline-queue-conflict', { detail: { ... } }))` and define the exact `detail` type.

## Strengths

- Clear ALLOWED-only footprint with explicit ‘STOP’ SHARED-gate check for `fake-indexeddb` (lines 31-34, 218-220).
- Good separation of concerns: library + hooks only, with explicit “ZERO route mounts / ZERO consumers” guardrails (lines 83-91, 210-215).
- FIFO-by-ID requirement and single-flight drain lock are explicitly stated (lines 59-62, 161-162), reducing race-condition ambiguity.
- 409 retention and conflictPending skip behavior are spelled out, including event emission and an explicit `resolveConflict` API (lines 69-73, 155-156).
- Test floor is pinned with a concrete minimum list rather than only “add tests” (lines 175-197).

## Warnings

None.
