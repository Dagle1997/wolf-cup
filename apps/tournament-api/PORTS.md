# Ports — tournament-api

Tracks files ported from Wolf Cup (`apps/api/**`) into tournament-api. Per the engine-boundary rule (FD-1 / FD-2), tournament-api may NOT import from Wolf Cup directly; instead, source files are copied with a provenance header and tracked here for future "re-sync against upstream" reviews.

When upstream Wolf Cup changes a ported source file, the `Last-checked-for-updates` column is the signal to re-evaluate whether the port needs an update.

| Target file | Source file | Source commit | Ported-on date | Deltas | Last-checked-for-updates |
|---|---|---|---|---|---|
| `apps/tournament-api/src/lib/ghin-client.ts` | `apps/api/src/lib/ghin-client.ts` | `3a38700303bd71a86c6de3621088fe161469d8b0` | 2026-04-27 | env reads via `src/lib/env.ts` (was `process.env`). **KNOWN LIMITATION:** state='WV' hardcoded upstream regardless of any future `?state=` query param value (preserved from source for v1 byte-for-byte parity). | 2026-04-27 |
| `apps/tournament-api/src/lib/pdf-gen.ts` + `apps/tournament-api/src/routes/pdf-schedule.ts` | **GREENFIELD — no Wolf Cup runtime source** | n/a | 2026-04-28 | T4-3 surveyed `apps/api/**` for runtime PDF generation; none exists. Wolf Cup's PDF artifacts (`reference/wolf-cup-*.html` → `.pdf`) are generated **offline** via shell-out to headless Chrome (`reference_pdf_generation.md` Claude memory; not runtime code). Production container has no Chrome (alpine-based). **Decision:** ship pdfkit-based renderer + GET endpoint, fully greenfield. **Alternatives rejected:** `puppeteer` (~280 MB), `@react-pdf/renderer` (React-tied), Chrome shell-out (Dockerfile bloat + no process-isolation), HTML+client-print (breaks Content-Type/Disposition AC). **Deps added (SHARED-gate, approved this story):** `pdfkit` (runtime) + `@types/pdfkit` (dev). | 2026-04-28 |
| `apps/tournament-api/src/db/schema/scoring.ts` (`holeScores` table only) | `apps/api/src/db/schema.ts` (`holeScores` table) | `f4dbb558a89d26efeaf4c9ebf7311fda91ed1e33` | 2026-04-28 | T5-1 schema-only port. Deltas: id integer-autoincrement → text UUID (FD-6); round_id/player_id integer → text (FK target type aligned); group_id REMOVED (tournament uses pairings → pairing_members for foursome context, T4-2); gross_score → gross_strokes (rename per epic AC line 1260; semantically more correct); ADDED scorer_player_id (FR-B10 attribution); ADDED client_event_id (FD-3/FD-5 offline idempotency); ADDED UNIQUE(round_id, player_id, hole_number, client_event_id) as ON CONFLICT DO NOTHING dedupe target; ADDED CHECK(gross_strokes >= 1) positivity guard. Other tables in `scoring.ts` (`rounds`, `score_corrections`, `round_states`, `scorer_assignments`) and the entire `audit.ts` (`audit_log`) are **GREENFIELD** — no Wolf Cup analogue. **Dual-UNIQUE behavior verified on libsql 0.17.0** via T5-1's `scoring.test.ts` Tests 4a/4b/4c (epic AC line 1273-1275 design honored). | 2026-04-28 |
| `apps/tournament-api/src/lib/r2-client.ts` | `apps/api/src/lib/r2-client.ts` | `2bb76900dee9c2b6221bc6fd4430987d700378ee` | 2026-05-05 | T7-4 port. Env reads via `src/lib/env.ts` (was `process.env`); ADDED `getSignedDownloadUrl` using `@aws-sdk/s3-request-presigner` (Wolf Cup's `R2_PUBLIC_URL` fast-path is intentionally NOT ported — tournament uses presigned GETs for all reads, forward-compatible with eventually moving the prefix to a private bucket); `uploadToR2` no longer returns the URL (just void) since callers presign on demand. Same R2 bucket as Wolf Cup per arch D5-10; tournament writes under `tournament/events/{eventId}/` prefix. | 2026-05-05 |
| `apps/tournament-api/src/routes/gallery.ts` | `apps/api/src/routes/gallery.ts` | `935f37e774448c6782b2660b2b037419e498fda7` | 2026-05-05 | T7-4 port. Event-centric schema (event_id + round_id NULLABLE; Wolf Cup keys by round_id only); R2 key prefix `tournament/events/{eventId}/{uuid}.{ext}`; signed-GET URLs replace Wolf Cup's `publicUrl` field; auth chain `requireSession + requireEventParticipant + (requireOrganizer for DELETE)` replaces Wolf Cup's `x-entry-code` bcrypt-compare (entry-code anonymous-upload model doesn't apply once SSO is in play); active-round resolution via `round_states.state IN ('in_progress','complete_editable')` ORDER BY `round_states.entered_at DESC` LIMIT 1 instead of `rounds.status='active'` (round_states is the tournament lifecycle source of truth from T5-1/T5-8); ADDED `bodyLimit({ maxSize: 12MB })` middleware as the request-level DoS guard; ADDED `isSafeEventId` defense-in-depth check on the bucket-key prefix; ADDED `gallery_photos` schema with `event_id` CASCADE / `round_id` SET NULL / `uploaded_by_player_id` RESTRICT FK posture (round-cancel preserves photos, mirrors Wolf Cup's 2026-04-06 fix). | 2026-05-05 |
