# Ports — tournament-web

Tracks files ported from Wolf Cup (`apps/web/**`) into tournament-web. Per the engine-boundary rule (FD-1 / FD-2), tournament-web may NOT import from Wolf Cup directly; instead, source files are copied with a provenance header and tracked here for future "re-sync against upstream" reviews.

When upstream Wolf Cup changes a ported source file, the `Last-checked-for-updates` column is the signal to re-evaluate whether the port needs an update.

| Target file | Source file | Source commit | Ported-on date | Deltas | Last-checked-for-updates |
|---|---|---|---|---|---|
| `apps/tournament-web/src/lib/offline-queue.ts` | `apps/web/src/lib/offline-queue.ts` | `ddf921b29afe9b6b50a1f136021502770b180e65` | 2026-04-28 | T5-3 [port]: DB rename `wolf-cup-offline` → `tournament-offline`; object store rename `score-queue` → `mutation-queue` (v1 carries 4 kinds: hole_score, sub_game_result, scorer_handoff, round_finalize); REMOVED ctp-queue store + 4 functions (CTP is Wolf Cup-only); REMOVED groupId / wolfDecision / autoCalculateMoney / entryCode fields; ADDED kind discriminator (4 v1 values); ADDED clientEventId (FD-3/FD-5; CALLER-supplied UUID v4); ADDED url + body fields (kind-agnostic dispatch); ADDED conflictPending + lastError + retryCount fields; ADDED mutation-queue-errored store (corrupted-entry quarantine); ADDED registerTerminalErrors / getTerminalErrors / `_resetTerminalErrorsForTests` / `_resetDbForTests` (test-only) / MAX_TRANSIENT_RETRIES = 5 / resolveConflict overload taking `overwriteBody`. **Dep added (SHARED-gate, approved this story):** `fake-indexeddb` (devDep, ~150KB; for jsdom test runs). | 2026-04-28 |
| `apps/tournament-web/src/hooks/useOfflineQueue.ts` | `apps/web/src/hooks/useOfflineQueue.ts` | `ddf921b29afe9b6b50a1f136021502770b180e65` | 2026-04-28 | T5-3 [port]: single-arg signature `useOfflineQueue(roundId)` (no groupId); generic kind-agnostic drain via `fetch(entry.url, ...)` instead of switch-on-shape; per-kind terminal-error registry consulted at drain time; universal failsafe at MAX_TRANSIENT_RETRIES (5) for transient 4xx; 5xx / network → BREAK + setTimeout(drain, 30s) heartbeat; 409 → CustomEvent('tournament-offline-queue-conflict') + entry retained with conflictPending; failsafe-purged emits CustomEvent('tournament-offline-queue-failsafe-purged'); REMOVED Wolf-Cup-specific hardcoded transient/terminal classifier; REMOVED entryCode header (tournament uses session-cookie auth via T1-6a). | 2026-04-28 |
| `apps/tournament-web/src/hooks/useOnlineStatus.ts` | `apps/web/src/hooks/useOnlineStatus.ts` | `ddf921b29afe9b6b50a1f136021502770b180e65` | 2026-04-28 | T5-3 [port]: identical logic to Wolf Cup; one safety guard added — `useState` initializer falls back to `true` if `navigator` is undefined (SSR/test edge case; Wolf Cup assumes browser context always). | 2026-04-28 |
